# AGENTS.md

AIエージェント向けのプロジェクト説明書です。ツール非依存の記述にしてあります。
（Claude Code は `CLAUDE.md` 経由でこのファイルを読み込みます）

## このプロジェクトは何か

RST主催のAIセミナー用ハンズオン教材です。
題材として、依存パッケージゼロの小さな**タスク管理API**を用意しています。
受講生はこのコードベースを Claude Code に触らせながら、AIコーディングの基本操作を体験します。

## 技術スタック

- Node.js 18+ / ESM (`"type": "module"`)
- 外部ランタイム依存は**ゼロ**（`node:http` と `node:test` のみ使用）
- テストランナーは Node 標準の `node --test`

外部ライブラリを追加しないでください。セミナー会場のネットワーク事情で
`npm install` が失敗すると、受講生全員が止まります。

## ディレクトリ構成

```
src/
  server.js     HTTPサーバー。ルーティングとリクエスト処理
  store.js      タスクのインメモリ永続化層
  validate.js   入力バリデーション
test/
  store.test.js
  validate.test.js
scripts/        セットアップ・診断スクリプト（セミナー運営用）
docs/           受講生向け演習と運営手順
```

## コマンド

| 目的 | コマンド |
|------|----------|
| テスト実行 | `npm test` |
| サーバー起動 | `npm start` （http://localhost:3000） |
| 環境診断 | `npm run doctor` |

## コーディング規約

- ESM の named export を使う。default export は使わない。
- セミコロンあり、シングルクォート、インデント2スペース。
- 関数名は動詞始まり（`createTask`, `validateTitle`）。
- エラーは例外を投げず、`{ ok: true, value }` / `{ ok: false, error }` の
  結果オブジェクトを返す（`src/validate.js` のパターンに揃える）。
- コメントは「なぜ」を書く。「何を」はコードで表現する。

## 変更時のルール

- `src/` を変更したら、対応する `test/` も必ず更新・追加する。
- 変更後は必ず `npm test` を実行し、結果を報告する。
- 既存のテストを、通すためだけに書き換えないこと。
  テストが失敗する場合、原因は実装側にある可能性を先に検討する。

## 触ってはいけないもの

- `scripts/` 配下 … セミナー運営用。受講生の演習対象外。
- `.devcontainer/` … 環境定義。壊すとコンテナが再生成できなくなる。
- `~/.bashrc`、環境変数 `ANTHROPIC_API_KEY` … 認証情報。読み取り・出力とも禁止。

## 既知の状態

セットアップ直後、`npm test` は**意図的に1件失敗します**。
これは演習3の教材です。勝手に修正せず、受講生から依頼されたときに対応してください。
