# AGENTS.md

AIエージェント向けのプロジェクト説明書です。ツール非依存の記述にしてあります。
（Claude Code は `CLAUDE.md` 経由でこのファイルを読み込みます）

## このプロジェクトは何か

RST主催のAIセミナー用ハンズオン環境です。
**受講生はプログラミング未経験者**で、Claude Code に触るのが初めての場合があります。

セミナー当日の流れは以下です。受講生は `README.md` を見ながら進めます。

```
ステップ1〜4  準備（Codespace 作成 → APIキー設定 → npm run doctor → claude 起動）
ステップ5     work-idea-hearing スキル
              受講生の仕事をヒアリングし、アイデアを3〜5個提案して1つ選んでもらう
ステップ6     vibe-app-builder スキル
              見た目の好みを聞き、apps/ に1枚のHTMLとして実装する
ステップ7     python3 preview.py で受講生のブラウザに表示して動作確認
ステップ8     見せあいっこ
```

**ステップ5〜6では、必ず対応するスキルに従ってください。** 自己流で進めないこと。

## 成果物のルール（最重要）

- 保存先は **`apps/` ディレクトリ**。ここ以外に置かない
- **1枚のHTMLファイル**（CSS/JSはインライン）で完結させる
- **サーバー・データベース・外部API呼び出しを使わない。**
  「AIっぽい」機能はテンプレート・パターン表・ルールベースの分岐で疑似的に実現する
- ファイル名は内容がわかる英数字（例: `nippou-generator.html`）

### 動作確認は preview.py で行う

Codespaces はコンテナ内で動いているため、HTMLファイルを開く操作では
受講生のブラウザに表示できません。必ず次を案内してください。

```bash
python3 preview.py
```

受講生には「**新しいターミナルを開いて**実行し、右下の通知の
『ブラウザーで開く』を押す」と伝えてください。
（`python` や `pip` はこの環境に存在しません。必ず `python3` と書くこと）

## 受講生への接し方

- **変更を加える前に、何をするつもりか1〜2文で説明する**
- IT用語・カタカナ専門用語を避け、たとえ話や具体例を使う
- 実装中にコードの詳細を延々と説明しない。**動いた結果を見せることを優先する**
- 詰まっている様子であれば、答えを出す前に「どこで詰まっているか」を確認する
- 「地味で驚きがない」は失敗。短時間でも「おっ」と思える見た目に仕上げる

## 技術スタック

- Node.js 22 / ESM (`"type": "module"`)
- Python 3.11（`preview.py` 用。標準ライブラリのみ）
- 外部ランタイム依存は**ゼロ**

**外部ライブラリを追加しないでください。** セミナー会場のネットワーク事情で
`npm install` が失敗すると、受講生全員が止まります。

## ディレクトリ構成

```
apps/           受講生の成果物（HTML）。ステップ6の出力先
preview.py      apps/ を 3000 番で配信するプレビューサーバー
.claude/skills/ ステップ5・6で使うスキル
scripts/        セットアップ・診断・リカバリ（セミナー運営用）
docs/           運営手順、および旧トラックの演習
src/ test/      【旧トラック】下記参照
```

## コマンド

| 目的 | コマンド |
|------|----------|
| 環境診断 | `npm run doctor` |
| プレビュー | `python3 preview.py` |
| Claude初期化のやり直し | `bash scripts/reset-claude.sh` |

## 触ってはいけないもの

- `scripts/` 配下 … セミナー運営用。受講生の演習対象外
- `.devcontainer/` … 環境定義。壊すとコンテナが再生成できなくなる
- `preview.py` … 起動時に出力する `http://localhost:3000/` の行は、
  Codespaces のポート自動検知に使われる。消さないこと
- `~/.bashrc`、環境変数 `ANTHROPIC_API_KEY` … 認証情報。読み取り・出力とも禁止

---

## 【旧トラック】src/ と test/ について

`src/` の**タスク管理API**は、エンジニア向けの旧演習（`docs/exercises.md`）の題材です。
**現行のセミナーでは使いません。** 受講生から明示的に依頼された場合のみ触ってください。

その場合に限り、以下の規約に従います。

- ESM の named export を使う。default export は使わない
- セミコロンあり、シングルクォート、インデント2スペース
- 関数名は動詞始まり（`createTask`, `validateTitle`）
- エラーは例外を投げず、`{ ok: true, value }` / `{ ok: false, error }` の
  結果オブジェクトを返す（`src/validate.js` のパターンに揃える）
- `src/` を変更したら、対応する `test/` も必ず更新・追加し、`npm test` を実行して報告する
- 既存のテストを、通すためだけに書き換えないこと

`npm test` は**意図的に1件失敗します**（旧演習3の教材）。
勝手に修正せず、受講生から依頼されたときに対応してください。
